#!/usr/bin/env bash
set -euo pipefail

output_dir="$HOME/docs/pictures/screenshots"
mkdir -p "$output_dir"

selection="$(slurp)"
file="$output_dir/$(date +'%Y-%m-%d_%H-%M-%S').png"

grim -g "$selection" "$file"
wl-copy < "$file"
printf '%s\n' "$file"
